<!DOCTYPE html>
<html lang="en">
<%- include partials/header -%>
<body>
    <h1>GamerList</h1>

    <main class="container">
        <div>
        <% games.forEach(v => { %>
            <div class="font"><%=v.title %></div>
            <img src="<%=v.img %>" height="100" />
        <% }) %>
        </div>
    </main>
</body>
</html>